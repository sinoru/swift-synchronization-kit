// Every entry point in this target is an always-inline function defined in the
// header. SwiftPM still requires the target to have at least one translation
// unit, so this file exists to provide one.

#include "CSynchronizationKitAtomic.h"
